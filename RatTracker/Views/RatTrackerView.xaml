<Window x:Class="RatTracker.Views.RatTrackerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:RatTracker.ViewModels"
        xmlns:controls="clr-namespace:RatTracker.Infrastructure.Controls"
        Style="{StaticResource WindowStyle}"
        mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:RatTrackerViewModel, IsDesignTimeCreatable=True}"
        Height="650" Width="950">
  <DockPanel>
    <Menu x:Name="MenuCopy" Height="19" VerticalAlignment="Top" DockPanel.Dock="Top">
      <MenuItem Header="Main Menu">
        <MenuItem Header="Send error report" />
        <MenuItem Header="Refresh EDDB data" />
      </MenuItem>
      <MenuItem Header="Settings" />
      <MenuItem x:Name="OverlayMenu" Header="Overlay" />
    </Menu>

    <Grid Margin="5">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <ContentControl Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" cal:View.Model="{Binding Rescues}" />
      <ContentControl Grid.Column="0" Grid.Row="1" cal:View.Model="{Binding AssignedRescue}" />

    </Grid>

  </DockPanel>
</Window>