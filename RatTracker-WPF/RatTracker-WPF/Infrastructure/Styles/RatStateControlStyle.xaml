<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:RatTracker_WPF.Infrastructure.Controls"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:converter="clr-namespace:RatTracker_WPF.Infrastructure.Converter"
                    mc:Ignorable="d">

  <Style TargetType="{x:Type controls:RatStateControl}">
    <Style.Setters>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type controls:RatStateControl}">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="RatName" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="Btn" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="Btn" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="Btn" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="Btn" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="Btn" />
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="23" />
              </Grid.RowDefinitions>
              <TextBlock Grid.Column="0" Text="{Binding State.Rat.Name, RelativeSource={RelativeSource TemplatedParent}}" />
              <Button Grid.Column="1" Content="FR" Command="{Binding FriendRequest, RelativeSource={RelativeSource TemplatedParent}}" Margin="2"
                      Background="{Binding State.FriendRequest, RelativeSource={RelativeSource TemplatedParent}, Converter={converter:RequestStateToColourConverter}}" />
              <Button Grid.Column="2" Content="WR" Command="{Binding WingRequest, RelativeSource={RelativeSource TemplatedParent}}" Margin="2"
                      Background="{Binding State.WingRequest, RelativeSource={RelativeSource TemplatedParent}, Converter={converter:RequestStateToColourConverter}}" />
              <Button Grid.Column="3" Content="SYS" Command="{Binding InSystem, RelativeSource={RelativeSource TemplatedParent}}" Margin="2"
                      Background="{Binding State.InSystem, RelativeSource={RelativeSource TemplatedParent}, Converter={converter:BooleanToColourConverter}}" />
              <Button Grid.Column="4" Content="BCN" Command="{Binding BeaconVisible, RelativeSource={RelativeSource TemplatedParent}}" Margin="2"
                      Background="{Binding State.Beacon, RelativeSource={RelativeSource TemplatedParent}, Converter={converter:BooleanToColourConverter}}" />
              <Button Grid.Column="5" Content="INST" Command="{Binding InInstance, RelativeSource={RelativeSource TemplatedParent}}" Margin="2"
                      Background="{Binding State.InInstance, RelativeSource={RelativeSource TemplatedParent}, Converter={converter:BooleanToColourConverter}}" />
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style.Setters>
  </Style>

</ResourceDictionary>